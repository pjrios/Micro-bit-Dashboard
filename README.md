# Micro-bit-Dashboard
Micro:bit Dashboard

## GitHub Pages
Double-click `prepare-github-pages.bat` to build into `docs/` for GitHub Pages. It will prompt for the repo name and set the correct base path.
